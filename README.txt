This project is waiting on approval from GGG to allow me Oauth2 access,
with just my account token the vast majority of endpoints are inaccessable.

Currently, boilerplate code exists demonstrating my caching system which reduces api calls by locally storing responses within the project folder. 

Later, relevant player information and oAuth tokens will be stored on a server instaed of locally.